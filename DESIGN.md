# 비전 검사 시스템 — 전체 설계 문서

> 압출 실 처짐 감지 자동화 시스템  
> 작성일: 2026-04  
> 현재 단계: Phase 1 개발 중

---

## 1. 프로젝트 목적

압출 공정에서 실이 지그에서 이탈하여 처질 경우,
USB 카메라가 이를 자동으로 감지하고 신호등(알람)을 울리는 시스템.

---

## 2. 전체 시스템 구성도

```
┌──────────────────────────────────────────────────────────────┐
│                        PC (Windows)                          │
│                                                              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │              Python 메인 프로그램                    │   │
│   │                                                     │   │
│   │  ① 영상 수신 (USB 카메라)                           │   │
│   │  ② ROI 영역 배경차분 분석                           │   │
│   │  ③ 처짐 감지 판단                                   │   │
│   │  ④ 상태 화면 표시 + 사진 자동 저장                  │   │
│   │  ⑤ Arduino로 시리얼 신호 전송                       │   │
│   └────────────────────────┬────────────────────────────┘   │
│                            │ USB Serial                      │
└────────────────────────────┼─────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   Arduino       │
                    │   Uno / Nano    │
                    └──┬──────────┬───┘
                       │ PWM      │ Digital
                       ▼          ▼
               ┌───────────┐  ┌──────────┐
               │ LED 드라이버│  │ 릴레이   │
               │ (조명 제어) │  │ 모듈     │
               └─────┬─────┘  └────┬─────┘
                     │              │
              ┌──────▼──────┐  ┌───▼──────────┐
              │  역광 조명  │  │  신호등 타워  │
              │  (LED 패널) │  │  🔴빨강: 알람 │
              │  + 수동다이얼│  │  🟢초록: 정상 │
              └─────────────┘  └──────────────┘
                     ↑
              [수동 다이얼 조광기]
              (수동 오버라이드)


카메라 설치 위치 (측면 역광 방식):

  [역광 LED 조명]           [USB 카메라]
         →→→    실/지그     👁
                 ||||
  
  실이 빛을 막아 실루엣으로 촬영됨
  → 실 색상/재질 무관하게 동일한 원리로 감지 가능
```

---

## 3. 하드웨어 설계

### 3-1. 카메라

| 항목 | 내용 |
|---|---|
| 종류 | USB 카메라 |
| 설치 위치 | 측면 (실을 옆에서 바라봄) |
| 촬영 방식 | 역광 (실루엣) |
| 해상도 | 1280 x 720 이상 권장 |

### 3-2. 조명 — 하이브리드 제어 방식

실 굵기가 0.2mm ~ 2.0mm (10배 차이)로 다양하여
밝기를 그때그때 조절해야 함.

```
┌────────────────────────────────────────────────┐
│           하이브리드 조명 제어                  │
│                                                │
│  [소프트웨어 프리셋]      [수동 다이얼]         │
│   Python에서 굵기         조광기 스위치로       │
│   선택 → Arduino PWM     물리적 오버라이드      │
│   신호로 밝기 자동 설정    (미세조정용)          │
│                                                │
│  평상시: 프리셋으로 빠르게 세팅                 │
│  미세조정 필요 시: 다이얼로 즉시 조절            │
└────────────────────────────────────────────────┘
```

**굵기별 밝기 프리셋 (Arduino PWM 0~255)**

| 굵기 | PWM 값 | 비고 |
|---|---|---|
| 0.2mm | 220 | 최고 밝기 (가는 실은 실루엣이 약함) |
| 0.5mm | 180 | |
| 1.0mm | 140 | |
| 1.5mm | 100 | |
| 2.0mm | 70 | 굵은 실은 조명 약해도 잘 보임 |

### 3-3. 신호등 연결 방식

```
PC → Arduino → 릴레이 모듈 → 신호등 타워

Arduino 핀 배정:
  Pin 6  : LED 조명 PWM (밝기 제어)
  Pin 8  : 초록 LED / 릴레이 (정상)
  Pin 9  : 빨강 LED / 릴레이 (알람)

시리얼 명령:
  '0' 전송 → 정상 (초록 ON, 빨강 OFF)
  '1' 전송 → 알람 (빨강 ON, 초록 OFF)
  'a'~'e'  → 굵기 프리셋 1~5 (조명 밝기 변경)
```

**신호등 타워 종류 선택 기준**

| 단계 | 추천 제품 | 전압 | 이유 |
|---|---|---|---|
| 개발/테스트 | USB 미니 신호등 | DC 5V | Arduino 직결, 안전 |
| 현장 운영 | 적층형 시그널 타워 | DC 24V | 공장 표준, 밝고 견고 |

---

## 4. 소프트웨어 설계

### 4-1. 감지 원리 (Phase 1 — OpenCV)

```
카메라 프레임 입력
       ↓
ROI 영역 잘라내기
(실이 처지면 들어오는 구역만 분석)
       ↓
배경 차분 (BackgroundSubtractorMOG2)
(정상 상태의 배경과 현재 프레임 비교)
       ↓
노이즈 제거 (모폴로지 연산)
       ↓
전경 픽셀 수 계산
       ↓
임계값 초과? → YES → 연속 N프레임 확인 → 알람
            → NO  → 정상
```

### 4-2. 모니터링 화면 구성

```
┌─────────────────────────────────────────────┐
│ STATUS: NORMAL          2026-04-20 14:32:01 │
│ Pixel: 123              Preset: 0.5mm       │
│                                             │
│  ┌──────────────────────────────────────┐   │
│  │          카메라 영상                 │   │
│  │   ┌──── ROI 박스 (초록/빨강) ─────┐  │   │
│  │   │  실이 처지면 이 안에서 감지   │  │   │
│  │   └──────────────────────────────┘  │   │
│  │                        ┌──────────┐ │   │
│  │                        │ BG Mask  │ │   │
│  │                        │ (서브창) │ │   │
│  │                        └──────────┘ │   │
│  └──────────────────────────────────────┘   │
│ [Q]종료 [S]저장 [1~5]프리셋 [R]배경초기화  │
└─────────────────────────────────────────────┘
```

### 4-3. 사진 자동 저장

| 저장 시점 | 파일명 형식 |
|---|---|
| 주기적 저장 (기본 5초) | `normal_20260420_143201_123.jpg` |
| 알람 발생 시 즉시 저장 | `alarm_20260420_143201_123.jpg` |
| 수동 저장 (S키) | `manual_20260420_143201_123.jpg` |

→ **이 사진들이 Phase 3 YOLOv8 학습 데이터가 됨**

---

## 5. 개발 로드맵

```
Phase 1 ✅ 현재
  OpenCV 배경차분 감지
  모니터링 화면 + 사진 자동 저장
  목표: PC + 카메라만으로 바로 테스트 가능
  ↓
Phase 2
  Arduino 연동
  - 신호등 타워 제어 (정상/알람)
  - LED 조명 PWM 제어 (굵기 프리셋)
  - 수동 다이얼 병행
  ↓
Phase 3  ← Phase 1이 자동으로 데이터 수집 중
  YOLOv8 학습 데이터 준비
  - data/captures/ 폴더의 사진들 활용
  - Roboflow로 라벨링 (웹에서 마우스로 박스 그리기)
  - 정상 / 처짐 두 클래스로 분류
  ↓
Phase 4
  YOLOv8 모델 학습 + Phase 1 감지 로직 교체
  - 조명 변화에 강한 AI 감지
  - 다양한 실 굵기/색상에 자동 대응
  - 이 파이프라인을 익히면 다른 검사에도 재활용 가능
  ↓
Phase 5
  Raspberry Pi 이식
  - Python 코드 거의 그대로 이식
  - GPIO 핀으로 릴레이 직접 제어 (Arduino 불필요)
  - 소형 전용 기기화 → 공장 상시 운영
```

---

## 6. Raspberry Pi 이식 계획

```
현재 (PC + Arduino)      →    최종 (Raspberry Pi 4)
─────────────────────────────────────────────────
PC: Python 실행              Pi: Python 실행 (동일)
USB 카메라                   USB 카메라 (동일)
Arduino: 릴레이 제어    →    Pi GPIO 핀: 직접 릴레이 제어
Arduino: PWM 조명       →    Pi GPIO PWM: 조명 제어
Windows 전용                 Linux (Raspberry Pi OS)
```

**Pi 이식 시 변경되는 코드 딱 두 군데:**
1. `settings.py` → `ARDUINO_PORT = "/dev/ttyUSB0"` 또는 GPIO 모드로 전환
2. 조명/신호등 제어 함수 → `RPi.GPIO` 라이브러리로 교체

나머지 감지 로직, UI, 저장 로직은 100% 동일.

---

## 7. YOLOv8 학습 파이프라인 (Phase 3~4 참고)

```
① 데이터 수집
   Phase 1 실행 중 data/captures/ 에 자동 저장됨
   정상 이미지 수백 장 + 알람(처짐) 이미지 수백 장 목표

② 라벨링 (Roboflow 사용 — 무료)
   https://roboflow.com
   웹에서 이미지 업로드 → 마우스로 처진 실 영역 박스 그리기
   클래스: "normal" / "sagging" (처짐)

③ 학습
   pip install ultralytics
   yolo train model=yolov8n.pt data=dataset.yaml epochs=100

④ 추론 교체
   Phase 1의 배경차분 로직을 YOLOv8 추론으로 교체
   → 조명 변화, 다양한 실 색상에 강한 감지

⑤ 재활용
   이 파이프라인 (수집→라벨링→학습→배포)을
   다른 비전 검사 항목에 그대로 적용 가능
```

---

## 8. 부품 리스트 (참고)

| 부품 | 용도 | 예상 비용 |
|---|---|---|
| USB 카메라 (기보유) | 촬영 | - |
| Arduino Uno 또는 Nano | 신호등/조명 제어 | 1~3만원 |
| 릴레이 모듈 2ch | 신호등 ON/OFF | 5천원 |
| LED 조명 패널 | 역광 조명 | 1~3만원 |
| 조광기 (다이얼) | 수동 밝기 조절 | 5천~1만원 |
| USB 미니 신호등 | 개발용 알람 출력 | 1~2만원 |
| 적층형 시그널 타워 | 현장용 알람 출력 | 3~10만원 |
| Raspberry Pi 4 (나중에) | 전용 기기화 | 8~12만원 |

---

*이 문서는 개발 진행에 따라 업데이트 예정*
